

export default function HeroSection(props){

    return (
        <div className="heroSectionEquipe GheroSection">
            <h1>{props.h1}</h1>
            <h4>{props.h4}</h4>
        </div>
    )
}